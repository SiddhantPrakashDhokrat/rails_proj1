<%= form_with(model: f1, local: true) do |form| %>
  <% if f1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(f1.errors.count, "error") %> prohibited this f1 from being saved:</h2>

      <ul>
        <% f1.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Name %>
    <%= form.text_field :Name %>
  </div>

  <div class="field">
    <%= form.label :Email %>
    <%= form.text_field :Email %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
